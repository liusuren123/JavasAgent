# T-PC-04: VisionEye 截屏定位

## 测试ID
T-PC-04

## 测试目的
验证 VisionEye 的 `locate_on_screen` 和 `find_target` 功能，确保目标定位和查找逻辑正确。

## 测试方法
使用 mock 验证 VisionEye 的目标定位逻辑：
- Mock ScreenAnalyzer、TargetMatcher 等底层组件
- 验证 VisionEye 在不同场景下的定位行为
- 验证精确查询和模糊查询的降级策略

## 测试场景及结果

| # | 场景 | 预期结果 | 实际结果 | 结果 |
|---|------|----------|----------|------|
| 1 | 目标存在 — 返回精确坐标 | 返回 bbox 坐标 | 返回 (100, 200, 300, 80) | ✅ PASS |
| 2 | 目标不存在 — 返回 None | 返回 None | 返回 None | ✅ PASS |
| 3 | 多目标精确查询 — 返回最佳匹配 | 返回置信度最高的目标 | 返回最高置信度匹配 | ✅ PASS |
| 4 | 模糊查询 — 返回语义最高分 | 返回语义相似度最高的目标 | 返回语义最高分匹配 | ✅ PASS |
| 5 | capture_and_analyze 完整流程 | 完整执行截屏→分析→定位 | 流程完整执行 | ✅ PASS |

## 测试结论
- **通过**: 5/5
- **状态**: ✅ PASS

## 备注
- 依赖状态：GroundingDINO/OpenCV 未安装，使用 mock 验证视觉管线逻辑
- 核心验证点：目标查找的精确/模糊匹配策略、多目标排序、完整管线流程
